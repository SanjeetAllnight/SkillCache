rules_version = '2';

// TESTING RULES — allow all reads and writes for debugging
// TODO: restore strict rules before production
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
